- pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

- contourpy>=1.3.1
- requirements.txt 
- cycler>=0.12.1
- filelock>=3.13.1
- fonttools>=4.56.0
- fsspec>=2024.6.1
- imageio>=2.37.0
- Jinja2>=3.1.4
- kiwisolver>=1.4.8
- lazy_loader>=0.4
- MarkupSafe>=3.0.2
- matplotlib>=3.10.0
- mpmath>=1.3.0
- networkx>=3.3
- numpy>=2.1.2
- packaging>=24.2
- pillow>=11.0.0
- pyparsing>=3.2.1
- python-dateutil>=2.9.0.post0
- scikit-image>=0.25.2
- scipy>=1.15.2
- setuptools>=70.2.0
- six>=1.17.0
- sympy>=1.13.1
- tifffile>=2025.2.18
- typing_extensions>=4.12.2



====================== Co lab setup ===========================
cutnet_test.py
===============

1. !git clone https://github.com/artsamir/bk_removal.git
%cd bk_removal

2. from google.colab import drive
drive.mount('/content/drive')

3. !ls /content/drive/MyDrive/cutnet_models/saved_models

4. paste the in cell : ---> paste colab cell for exp/cutnet_colab_test.py


===================
cutnet_train.py:
==================

1. Check it first cutnet_test.py (1, 2, 3) done

2. Paste it in cell (if masks_images given "false" the do below (3, 4, 5))
dataset_path = "/content/bk_removal/my_dataset"
input_images_path = os.path.join(dataset_path, "input_images")
masks_images_path = os.path.join(dataset_path, "masks_images")
image_ext = ('.jpg', '.jpeg', '.png')  # Add .png if needed
label_ext = '.png'


print("Dataset folder exists:", os.path.exists(dataset_path))
print("Input images folder exists:", os.path.exists(input_images_path))
print("Masks images folder exists:", os.path.exists(masks_images_path))

# List the files inside input_images and masks_images
if os.path.exists(input_images_path):
    print("Input images:", os.listdir(input_images_path))
else:
    print("❌ 'input_images' folder is missing!")

if os.path.exists(masks_images_path):
    print("Masks images:", os.listdir(masks_images_path))
else:
    print("❌ 'masks_images' folder is missing!")



3. os.makedirs("/content/bk_removal/my_dataset/masks_images", exist_ok=True)
print("✅ Created missing 'masks_images' folder!")

4. from google.colab import files
uploaded = files.upload()  # Select multiple mask images


5. import shutil
for filename in uploaded.keys():
    shutil.move(filename, "/content/bk_removal/my_dataset/masks_images/")

6. paste it in cell : find --> paste colab cell for exp/cutnet_colab_test.py
